[include]
	# for user and github config
	path = ~/.gitconfig.private
[core]
	editor = vim
[branch]
	sort = -committerdate
[tag]
	sort = -version:refname
[diff]
	algorithm = histogram
	renames = true
[color]
	ui = true
[alias]
	graph = log --graph --all --decorate --oneline
[push]
	default = simple
[commit]
	verbose = true
