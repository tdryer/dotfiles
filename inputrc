# use vi keybindings for bash/readline
# except for python and ruby repls, which have problems with it
$if ruby
$else
    $if python
    $else
        set editing-mode vi
    $endif
$endif

# ctrl l to clear screen in vi mode
Control-l: clear-screen
